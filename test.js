const { Cell } = require('@ton/core'); const fs = require('fs'); const data = JSON.parse(fs.readFileSync('minter_data.json', 'utf8')).data; const c = Cell.fromBoc(Buffer.from(data, 'hex'))[0]; const cs = c.beginParse(); console.log('Coins:', cs.loadCoins()); console.log('Admin:', cs.loadAddress()); console.log('Content Ref:', cs.loadRef()); console.log('Wallet Ref:', cs.loadRef()); console.log('Remaining bits:', cs.remainingBits, 'Remaining refs:', cs.remainingRefs);
